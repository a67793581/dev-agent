# GitHub Pages 使用 Jekyll 构建，零运行成本
# 在仓库 Settings → Pages → Source 选择 "Deploy from a branch" → 分支选 main → 目录选 /docs

title: DevAgent
description: AI 驱动的命令行编程 Agent，Go 实现，ReAct 循环与自定义工具调用
theme: jekyll-theme-cayman
# 可选：使用 GitHub 默认主题 minima
# theme: minima
